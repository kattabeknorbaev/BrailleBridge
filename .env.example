# Optional. Without these, BrailleBridge still works fully in the browser
# (translation, PDF/Word import, on-device OCR); only cloud OCR and the
# feedback form are disabled.
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-public-key"
